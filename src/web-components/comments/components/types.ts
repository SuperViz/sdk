export type AnnotationOptions = {
  resolvable?: boolean;
  resolved?: boolean;
}

export enum CommentDropdownOptions {
  EDIT = 'Edit',
  DELETE = 'Delete',
}

export enum CommentMode {
  EDITABLE = 'editable',
  READONLY = 'readonly',
}
