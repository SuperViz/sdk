import { css } from 'lit';

export const commentsStyle = css`
  div.sv-container {
    position: absolute;
    top: 0;
    right: 0;
    height: 100%;
    width: 320px;
    background: var(--sv-white);
    color: var(--sv-gray-700);
  }
`;
